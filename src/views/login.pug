extends base


block content
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="아이디", name="username", type="text", required)
        input(placeholder="비밀번호", name="password", type="password", required)
        input(type="submit", value="로그인")
        br
        a(href="users/github/start") 깃허브로 로그인하기 &rarr;
    hr
    div
        span 계정이 없으신가요?  
        a(href="/join") 계정 만들기 &rarr; 